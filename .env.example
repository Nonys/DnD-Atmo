OPENAI_API_KEY=<api-key>

# Image generation model: gpt-image-1 (default) or dall-e-3
IMAGE_MODEL=gpt-image-1

# Image quality:
#   gpt-image-1 → low | medium (default) | high
#   dall-e-3    → standard (default) | hd
IMAGE_QUALITY=medium